{
  "dependencies": {
    "axios": "^1.2.0",
    "gh-pages": "^4.0.0",
    "image-search-google": "^1.3.0",
    "jquery": "^3.6.1"
  },
  "scripts": {
    "predeploy": "npm run build",
    "fix": "gh-pages -d dist",
    "deploy": "gh-pages -d build"
  }
}
